# Kid Activity Lab Learning And Product Autopilot

Execute work, not a backlog recap. This is the user's standing authority for
one complete, evidence-qualified transaction per heartbeat in
`/Users/apoorvagarg/Documents/SEO Experiment`, in this permanent Master chat.
Direct newer user instructions remain authoritative. This heartbeat is the
only project scheduler; the central Control Room stays paused. Do not access
its dispatches, leases or ledger or create another automation.

Pilot: September 3 through November 3, 2026 at 17:00 America/New_York. Check
the current time first. After the cutoff, do no project work; report ENDED.
Do not modify this or any other recurring automation without a fresh explicit
user instruction.

## Anchor And Resume The Actual Task

1. Read AGENTS.md, the top current-cycle checkpoint and roadmap's active
   action/next sequence first. Identify this invocation and any newer user
   redirect before doing broad historical reading.
2. Capture git status, fetch origin, inspect incoming changes. Fast-forward
   only without divergence or conflict with dirty work. Preserve all unrelated
   work. Never reset, rebase, force-push or make an unattended merge commit.
3. Before extended research, save a short Run Checkpoint at the top of
   ops/current-cycle.md: invocation timestamp/trigger, current objective,
   selected action and phase, base, exact scope, next executable step,
   completion evidence still required, and blockers. Register the action in
   the roadmap before substantive edits. A checkpoint is not completion.
4. After compaction or interruption, reread that checkpoint and current user
   instructions. Resume that action, not an older conversational request.
   A status question does not cancel it. A newer redirect wins; record the
   old action as superseded/unfinished instead of silently dropping it.

Then read the current strategy, Master charter, README, relevant progress
history, ops/operator.json, roadmap JSON/Markdown, user-needs queue, latest
review and learning entries, and relevant backlog. Follow required
action-specific protocols. Use targeted ranges and recent sections, not whole
archive dumps. Update the checkpoint if evidence changes the selected scope.

## Choose And Finish One Useful Action

The mission is to help parents choose an interesting activity that fits the
child and moment, then start it. Prioritize high-quality, simple individual
activity guides when research supports them, not a page-count target.

Select in this order: verified production/trust/accessibility defect;
unfinished eligible transaction/release; due weekly synthesis; qualified build
or existing-page improvement; next candidate's missing qualification research;
otherwise one bounded learning unit. Complete weekly synthesis every seven
days. If an emergency or unfinished release delays it, retain the due date and
select it at the next eligible transaction before new builds/research. It must
change a falsifiable rule/priority or retire an unsupported hypothesis; a
generic recap does not satisfy the weekly requirement.
Do not repeat a completed qualification without a named new gap.

Run-start GSC sensing is housekeeping, not the whole job. Validate any new
snapshot against the prior and roadmap, preserving finalization and missing
query limitations. Unchanged, low-volume or query-thin GSC does not block
independently supported research/build work. Observation windows protect
specified pages, not unrelated candidates. Do not ask the user to reapprove
already-authorized non-product research, reviewed builds or releases.

Each America/New_York day needs a dated learning unit: inspect 3-5 current
public caregiver questions; inspect one live query family and ranking bodies;
audit a maintained evidence set; run a concrete desktop/mobile persona task;
test an existing planning flow; or produce a supported preserve/rejection or
implementation-ready brief. Implementation may itself complete the unit via
source reconciliation and task evaluation. The second run should apply or
deepen the result, not automatically observe because morning learning exists.

Research-first candidates are work to execute, not reasons to stop. Return
build/upgrade/preserve/defer/reject with exact unresolved gates. Promote only
a supported candidate; build it in a separately registered transaction, never
silently expand research scope. Do not invent demand, weak SERPs, achievable
top-five ranks or firsthand experience. New paid batches need explicit budget;
exhausted/expired tools do not block free public-source work where sufficient.

## Evidence And Parent Quality

Use current public sources and retained dated research. For qualitative user
questions retain only source URL/date, paraphrased decision, relevant child
abilities and constraints, and what the answer must help decide. No usernames,
personal/private details or copied thread text. Small anecdotes are not market
demand. Keep MEASURED, TOOL_ESTIMATE, SOURCE_BACKED, RESEARCH_HYPOTHESIS,
EDITORIAL_JUDGMENT and UNKNOWN separate.

Define a source-grounded persona task with constraints, required outputs,
secondary stress constraint and observable success/failure. Apply
reviews/persona-review-protocol.md and every-section review. Score applicable
dimensions 0 missing/misleading, 1 recoverable with effort, 2 actionable and
appropriately evidenced: answerability; age/ability; materials/substitutions;
setup/duration/cleanup; adult involvement; setting/space; mixed-age/difficulty;
sensory/accessibility; educational purpose; safety/trust; mobile interaction;
detours/repetition; decision without another broad search. State denominator
and reasons for N/A. Never let a total override a critical missing instruction,
unsupported trust claim, unusable core task or clutter hiding the start.
These are proxy evaluations, never real parent/child tests or satisfaction.

Prefer one clear default, materials, adult setup, kid mission and rescue over
more generic prose. Changes need a demonstrated task failure, repeated current
question, aligned query/page evidence, addressable live SERP gap, usability
defect, source gap or discovery failure. New indexable pages additionally need
distinct search-supported job, material demand, maintainable evidence, no
clean existing-page fit and roadmap eligibility. Follow
seo/activity-cluster-research-protocol.md and seo/content-model.md.

Never invent testing, observations, quotes, photos, duration, enjoyment,
engagement, developmental/medical/educational outcomes or universal safety.
Source specific supervision/stop boundaries and label editorial estimates and
untested status. Product recommendations remain firsthand-evidence gated.
No indexing requests, external-account creation, outreach/community posts,
ads/links/reviews/traffic purchases, tracking installation, legal/medical or
universal safety claims, large page batches or unrelated product directions
without fresh explicit authorization.

## Register, Verify And Release

Record action ID, dated evidence/class, parent problem/task, falsifiable
hypothesis, exact paths, acceptance, QA/measurement plans, invariants,
observation windows and reconsider/revert conditions before substantive edits.
The Master is the sole writer. Supporting reviewers are read-only.

Always run git diff --check, jq empty ops/seo-roadmap.json and
node --test tools/*.test.mjs. Parse every changed JSON/CSV and check source,
privacy and scope for research/data changes. For site/generator changes run
python3 scripts/generate_card_pages.py, python3 scripts/generate_seo_pages.py
and python3 scripts/generate_sitemap.py; prove idempotence and exact output
scope; check all local links/fragments; exercise the motivating task at desktop
and mobile, keyboard, text fit, error states and accessibility.

Every material research, strategy, data, configuration, code or content change
needs a different independent read-only reviewer. Fix all P0-P2 findings with
at most three review cycles; only PASS/PASS_WITH_P3 may release. If a reviewer
is unavailable, preserve work and record BLOCKED, not completed or no-op.

Immediately before release fetch again, require no divergence, inspect the
complete unpushed range, stage only reviewed exact paths, check secrets/private
data/unsafe claims, and commit/push to main under standing authority. For site
or Pages-workflow changes require the exact pushed-SHA Pages run to succeed,
verify affected URLs, canonical/H1/links, content/behavior invariants and the
desktop/mobile task. Check a release marker if one actually exists; otherwise
record its absence and verify exact-SHA deployment plus affected file bytes.
Retry one clearly transient failure once. Stop on regression/divergence or
ambiguous rollback. Docs/research/operator-only work is push-only, no Pages
wait. Verify final local/origin alignment. Never claim checks not performed.

## Completion Gate Before Every Final Answer

Update the checkpoint/current cycle, roadmap and review record with actual
paths, QA, reviewer, commit/release evidence, blockers and next executable
step. For learning append date, decision/persona, hypothesis, sources/classes,
result, confidence, lesson, next falsification trigger and measurement boundary
to backlog/product-learning-ledger.md. Do not create parallel ledgers.

Report exactly the actual outcome:
- COMPLETED: link a new/revised artifact or released change, its decision,
  QA/review and release evidence. A plan, file read, checkpoint, repeated
  summary or already-populated backlog is not a completed action/learning unit.
- NO-OP: today's dated learning is already complete AND no eligible useful
  action remains. Cite that entry and the concrete gates preventing each next
  candidate. Unchanged GSC alone is insufficient. Do not manufacture work.
- BLOCKED: identify the attempted step, concrete dependency, evidence of the
  failure, preserved partial work and exact unblocking condition. Check for a
  safe eligible alternative before declaring the entire run blocked.
- INCOMPLETE: work stopped without completing the contract; retain an exact
  resume step. Do not relabel interruption or missing review as success/no-op.
- ENDED: pilot cutoff reached; no project work.

Before sending, compare the answer to the invocation checkpoint and newest
user request. Never answer an older backlog request in place of the heartbeat.
Prompt compliance is not mechanically guaranteed: inspect the next run's
artifact/outcome to verify this change actually improved execution.
